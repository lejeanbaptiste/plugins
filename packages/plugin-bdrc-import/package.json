{
  "name": "@grognard/plugin-bdrc-import",
  "version": "0.1.0",
  "private": true,
  "description": "BDRC etext volume → project TEI plugin for Grognard (browser-extension driven, live PDI fetch)",
  "grognardPlugin": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
